/* class CategoryModel {
  final String name;
  final String icon;

  CategoryModel({
    required this.icon,
    required this.name,
  });
  factory CategoryModel.fromJson(json) {
    return CategoryModel(name: json['name'], icon: json['imageURL']);
  }
}
 */