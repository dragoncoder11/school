import 'package:flutter/material.dart';

class smallVirticalSpace extends StatelessWidget {
  const smallVirticalSpace({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 7,
    );
  }
}

class bigvirticalSpace extends StatelessWidget {
  const bigvirticalSpace({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}
